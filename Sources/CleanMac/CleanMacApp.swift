import SwiftUI

/// CleanMac application entry. Minimal shell: features are cut as
/// deliberate slices per tasks/todo.md, never accumulated here.
@main
struct CleanMacApp: App {
    var body: some Scene {
        WindowGroup {
            Text("CleanMac")
        }
    }
}
