/// CleanMacCore — engine library for the native CleanMac app.
///
/// Native-first: pure Swift + Foundation. No third-party dependencies.
/// Every destructive path goes through `Safety` (see Task 1).
public enum CleanMacCore {
    /// Semantic version of the engine. Bumped per release.
    public static let version = "0.1.0"
}
